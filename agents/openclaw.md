# OpenClaw Adapter

- Register `SKILL.md` as the primary instruction source.
- Map OpenClaw tools to runtime operations only.
- Do not create a separate coaching policy.
- If a reference is unavailable, follow the conservative fallback in `SKILL.md`.
